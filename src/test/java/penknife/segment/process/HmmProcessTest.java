package penknife.segment.process;

import org.junit.Test;
import penknife.segment.logic.Term;

import java.util.List;

public class HmmProcessTest {

    String s = "碧桂园做事靠谱，理性，而恒大的老许像个疯子，29元还接盘万科，12元收购盛京银行，100亿香港买楼";

    @Test
    public void doTest() {

        /*DAG dag = new DAG(s);
        List<Term> result = dag.splitByPath();
        HmmProcessor hmmProcessor = new HmmProcessor();
        result.forEach(r -> System.out.print(r.word + "|"));
        System.out.println("\n");
        result = hmmProcessor.process(result);
        result.forEach(r -> System.out.print(r.word + "|"));*/
    }
}
