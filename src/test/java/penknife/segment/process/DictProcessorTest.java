package penknife.segment.process;


import org.junit.Test;

public class DictProcessorTest {

    @Test
    public void doTest() {
        /*DictProcessor dictProcessor = new DictProcessor();
        List<Term> taggedWordList = Lists.newArrayList(TaggedWord.of("一"),
                TaggedWord.of("周"), TaggedWord.of("年"), TaggedWord.of("纪"), TaggedWord.of("念"));

        List<Term> taggedWords = dictProcessor.process(taggedWordList);

        taggedWords.forEach(w -> System.out.print(w.word + "|"));*/
    }
}
